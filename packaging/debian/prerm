#!/usr/bin/env bash
set -euo pipefail

# 视频由普通用户启动器管理。升级时停止它，避免覆盖私有运行时文件时旧进程
# 仍在使用它们；绝不触碰小车原有的 Supervisor 服务。
case "${1:-}" in
  upgrade|remove|deconfigure)
    /usr/bin/ry-aletheia-video stop >/dev/null 2>&1 || true
    ;;
esac
